var div = $("#manual-images");
var images = "<% @manual.images.each do |image| %><%= escape_javascript(render(:partial => 'manuals/image', :locals => { image: image })) %><% end %>";
div.html(images);
$("#manual-images-container").removeClass("loading").spin(false);